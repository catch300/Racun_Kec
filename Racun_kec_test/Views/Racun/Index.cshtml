

@using Racun_kec_test.Models.MvcPaging
@using Racun_kec_test.Models
@model IPagedList<Racun>



@{
    ViewBag.Title = "Lista računa";
}



@section Header{
    <script>
        var sorter_options = {
            headers: {
                3: { sorter: 'datetime' },
                4: { sorter: 'decimal' },
                5: { sorter: 'decimal' }
            }
        };
    </script>
}

<h1>Lista računa</h1>

<p class="noprint">
    @Html.ActionLink("[+] Create New ", "Create", new { @class = "btn primary" })
</p>

@{Html.RenderPartial("SearchBoxItems", "Racun");}

<div class="pagingContainer noprint">
    @{
        RouteValueDictionary r = new RouteValueDictionary();
        r.Add("to", Request["to"]);
        r.Add("from", Request["from"]);
        r.Add("text", Request["text"]);
    }
    @Html.Pager(Model.PageSize, Model.PageNumber, Model.TotalItemCount, r)
</div>

<table class="zebra-striped">
    <thead>
    <th>
        ID
    </th>
    <th>
        Kupac
    </th>
    <th>
        Oznaka
    </th>
    <th>
        Datum izdavanja
    </th>
    <th>
        Neto
    </th>


    <th>
        Ukupno za platiti
    </th>

    <th class="noprint"></th>
    </thead>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                #@Html.DisplayFor(modelItem => item.broj_racuna)
            </td>
            <td>
                <a href="@Url.Action("Details", "Customer", new { id=item.id_kupac})">
                    @Html.DisplayFor(modelItem => item.Kupac.ime_prezime)
                </a>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.oznaka)
            </td>
            <td nowrap>
                @Html.DisplayFor(modelItem => item.datum_izdavanja)
            </td>
            <td nowrap>
                @String.Format("{0:C}", item.Stavke_Racuna.Sum(i => i.Ukupno))
            </td>
            @* <td>
                    @String.Format("{0:C}", item.InvoiceDetails.Sum(i => i.TotalPlusVAT))
                </td>*@

            <td nowrap>
                @String.Format("{0:C}", item.UkupnoplusPDV)



            </td>
            <td class="noprint" nowrap>
                @Html.ActionLink("Edit", "Edit", new { id = item.id_racun }, new { @class = "btn primary" })
                @Html.ActionLink("Pregled računa", "Print", new { id = item.id_racun }, new { target = "_blank", @class = "btn" })
                @Html.ActionLink("Delete", "Delete", new { id = item.id_racun }, new { @class = "btn" })
                @Html.ActionLink("PRINT", "PrintViewToPdf", new { id = item.id_racun }, new { target = "_blank", @class = "btn" })


            </td>
        </tr>
    }



</table>
<!-- GLOBAL SCRIPTS -->
<script src="~/assets/plugins/jquery-2.0.3.min.js"></script>
<script src="~/assets/plugins/bootstrap/js/bootstrap.min.js"></script>
<script src="~/assets/plugins/modernizr-2.6.2-respond-1.1.0.min.js"></script>
<!-- END GLOBAL SCRIPTS -->
<!-- PAGE LEVEL SCRIPTS -->
<script src="~/assets/plugins/dataTables/jquery.dataTables.js"></script>
<script src="~/assets/plugins/dataTables/dataTables.bootstrap.js"></script>
<script>
    $(document).ready(function () {
        $('#dataTables-example').dataTable();
    });
</script>
